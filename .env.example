# Optional but strongly recommended. Increases GitHub's REST API allowance and
# must only be configured as a server-side environment variable.
GITHUB_TOKEN=

# Public production origin used in canonical links, feeds, sitemaps, and social
# cards. The hosted GistBlog URL is used when this is omitted.
NEXT_PUBLIC_SITE_URL=https://gistblog.vercel.app
